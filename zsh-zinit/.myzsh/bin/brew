#!/bin/bash
# 判断是Linux还是Mac os

if [[ ! -e "${HOMEBREW_REPOSITORY}" ]]; then
  echo 'YuSoLi: brew not installed. Start installing...'
  bash "${MYZSH}"/sh/install-brew.sh
fi

# eval $("${HOMEBREW_REPOSITORY}"/bin/brew shellenv) #ckbrew

"${HOMEBREW_REPOSITORY}"/bin/brew $@
